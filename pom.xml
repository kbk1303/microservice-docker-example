<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  
  <modelVersion>4.0.0</modelVersion>
  <groupId>local.lyngberg.microservice.docker</groupId>
  <artifactId>microservice-docker-parent</artifactId>
  <version>${revision}</version>
  <packaging>pom</packaging>
  <name>microservice-docker-parent</name>

      <!-- ===== Modules ===== -->
    <modules>
        <module>bc-login/login-api</module>
        <module>bc-login/login-impl</module>
        <module>bc-auditlog/auditlog-api</module>
        <module>bc-auditlog/auditlog-impl</module>
        <module>common-web</module>
        <module>shared-kernel</module>
    </modules>
       


      <!-- ===== Properties ===== -->
    <properties>
        <mockito.version>5.20.0</mockito.version>
        <maven.surefire.plugin.version>3.5.4</maven.surefire.plugin.version>
        
        <!-- Lader Maven starte tests med Mockito som Java agent -->
        <mockito.agent.argLine>
            -javaagent:${settings.localRepository}/org/mockito/mockito-core/${mockito.version}/mockito-core-${mockito.version}.jar
        </mockito.agent.argLine>
        <test.jvm.args.extra>-Xshare:off</test.jvm.args.extra>
        <revision>0.0.1-SNAPSHOT</revision>
        <spring-boot.version>3.5.12</spring-boot.version>
        <java.version>21</java.version>
        <spring-boot.run.skip>true</spring-boot.run.skip>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <maven.compiler.release>${java.version}</maven.compiler.release>
        <maven.compiler.plugin.version>3.14.1</maven.compiler.plugin.version>
        <!-- Reproducible builds (optional) -->
        <project.build.outputTimestamp>2025-01-01T00:00:00Z</project.build.outputTimestamp>

        <!-- Centralized versions -->
        <archunit.version>1.4.1</archunit.version>
        <enforcer.version>3.5.0</enforcer.version>
        <extra.enforcer.version>1.11.0</extra.enforcer.version>
        <org.testcontainers>1.21.3</org.testcontainers>
    </properties>

    <!-- ===== Dependency Management ===== -->

    <dependencyManagement>
        <dependencies>
        <!-- Spring Boot BOM (root aggregator, not starter-parent) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-dependencies</artifactId>
            <version>${spring-boot.version}</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>

        <!-- ArchUnit (JUnit 5) -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>${archunit.version}</version>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5-engine</artifactId>
            <version>${archunit.version}</version>
        </dependency>
        <!-- Mockito core for advanced usages -->
                <dependency>
                    <groupId>org.mockito</groupId>
                    <artifactId>mockito-core</artifactId>
                    <version>${mockito.version}</version>
                </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- ===== Inherited test deps for all children ===== -->
    <dependencies>
    
        <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-test</artifactId>
        <scope>test</scope>
        </dependency>

        <dependency>
        <groupId>com.tngtech.archunit</groupId>
        <artifactId>archunit-junit5</artifactId>
        <scope>test</scope>
        </dependency>
        <!-- Mockito core for advanced usages -->
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <scope>test</scope>
            </dependency>
    </dependencies>

    <build>
        <!-- Version pinning only -->
        <pluginManagement>
        <plugins>
            <plugin>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-maven-plugin</artifactId>
            <version>${spring-boot.version}</version>
            <configuration>
                <skip>${spring-boot.run.skip}</skip>
            </configuration>
            </plugin>
            <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <version>${maven.compiler.plugin.version}</version>
            </plugin>
            <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <version>${maven.surefire.plugin.version}</version>
            </plugin>
            <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-failsafe-plugin</artifactId>
            <version>${maven.surefire.plugin.version}</version>
            </plugin>
            <plugin>
            <groupId>org.jacoco</groupId>
            <artifactId>jacoco-maven-plugin</artifactId>
            <version>0.8.12</version>
            </plugin>
            <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-enforcer-plugin</artifactId>
            <version>${enforcer.version}</version>
            <dependencies>
                <dependency>
                <groupId>org.apache.maven.enforcer</groupId>
                <artifactId>enforcer-rules</artifactId>
                <version>${enforcer.version}</version>
                </dependency>
                <dependency>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>extra-enforcer-rules</artifactId>
                <version>${extra.enforcer.version}</version>
                </dependency>
            </dependencies>
            </plugin>
        </plugins>
        </pluginManagement>

        <plugins>
      <!-- Compiler (inherited by children) -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <release>${java.version}</release>
            <compilerArgs>
              <arg>-proc:full</arg>
          </compilerArgs>
        <annotationProcessorPaths>
          <!-- Spring Boot config metadata -->
          <path>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
          </path>
        </annotationProcessorPaths>
        </configuration>
        <!-- Disable processing for tests -->
        <executions>
          <execution>
            <id>default-testCompile</id>
            <goals><goal>testCompile</goal></goals>
            <configuration>
              <compilerArgs>
                <arg>-proc:none</arg>
                <!-- Optional: hide the warning line altogether -->
                <!-- <arg>-Xlint:-options</arg> -->
              </compilerArgs>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Root Surefire: pass ${argLine}; exclude E2E/IT by default -->
      <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-surefire-plugin</artifactId>
      <configuration>
        <argLine>${surefire.jacoco.args} ${mockito.agent.argLine} ${test.jvm.args.extra}</argLine>
        <useModulePath>false</useModulePath>
        <useManifestOnlyJar>false</useManifestOnlyJar>
         <excludes>
          <exclude>**/*IT.java</exclude>
        </excludes>
        <excludedGroups>e2e</excludedGroups>
      </configuration>
    </plugin>

    <!-- JaCoCo ONLY in root: agent (UT+IT), merge, aggregate report, and check -->
    <plugin>
      <groupId>org.jacoco</groupId>
      <artifactId>jacoco-maven-plugin</artifactId>
      <version>0.8.14</version>
      <executions>

        <!-- 1) UNIT TEST AGENT (all modules) -->
        <execution>
          <id>before-unit-test-execution</id>
          <goals>
            <goal>prepare-agent</goal>
          </goals>
          <configuration>
            <propertyName>surefire.jacoco.args</propertyName>
            <destFile>${project.build.directory}/jacoco-output/jacoco-unit-tests.exec</destFile>
          </configuration>
        </execution>

        <!-- 2) INTEGRATION TEST AGENT (all modules) -->
        <execution>
          <id>before-integration-test-execution</id>
          <phase>pre-integration-test</phase>
          <goals>
            <goal>prepare-agent</goal>
          </goals>
          <configuration>
            <propertyName>failsafe.jacoco.args</propertyName>
            <destFile>${project.build.directory}/jacoco-output/jacoco-integration-tests.exec</destFile>
          </configuration>
        </execution>

        <!-- 3) PER-MODULE MERGE (UT+IT -> merged.exec) -->
        <execution>
          <id>merge-unit-and-integration</id>
          <phase>post-integration-test</phase>
          <goals>
            <goal>merge</goal>
          </goals>
          <configuration>
            <fileSets>
              <fileSet>
                <directory>${project.build.directory}/jacoco-output/</directory>
                <includes>
                  <include>*.exec</include>
                </includes>
              </fileSet>
            </fileSets>
            <destFile>${project.build.directory}/jacoco-output/merged.exec</destFile>
          </configuration>
        </execution>

        <!-- 4) PER-MODULE REPORT (merged.exec -> site/jacoco-merged-test-coverage-report) -->
        <execution>
          <id>create-merged-report</id>
          <phase>verify</phase>
          <goals>
            <goal>report</goal>
          </goals>
          <configuration>
            <dataFile>${project.build.directory}/jacoco-output/merged.exec</dataFile>
            <outputDirectory>
              ${project.reporting.outputDirectory}/jacoco-merged-test-coverage-report
            </outputDirectory>
          </configuration>
        </execution>
      </executions>
    </plugin>

    
      <!-- Enforcer: hygiene rules -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <executions>
          <execution>
            <id>enforce</id>
            <goals><goal>enforce</goal></goals>
            <configuration>
              <rules>
                <requireJavaVersion>
                  <version>[21,)</version>
                </requireJavaVersion>
                <requireMavenVersion>
                  <version>[3.9,)</version>
                </requireMavenVersion>
                <dependencyConvergence/>
                <banDuplicateClasses/>
                <enforceBytecodeVersion>
                  <maxJdkVersion>${java.version}</maxJdkVersion>
                </enforceBytecodeVersion>
                <requireNoRepositories/>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
       <!-- failsave plugin-->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<configuration>
					<argLine>${failsafe.jacoco.args} ${mockito.agent.argLine} ${test.jvm.args.extra}</argLine>
          <useModulePath>false</useModulePath>
          <useManifestOnlyJar>false</useManifestOnlyJar>
          <!-- Kun ITs her -->
          <includes>
            <include>**/*IT.java</include>
          </includes>
				</configuration>
        <executions>
					<execution>
            <id>failsafe-it</id>
            <phase>integration-test</phase>
            <goals>
              <goal>integration-test</goal>
            </goals>
          </execution>

          <execution>
            <id>failsafe-verify</id>
            <phase>verify</phase>
            <goals>
              <goal>verify</goal>
            </goals>
          </execution>
				</executions>
			</plugin>
        </plugins>

    </build>

    <!-- ===== Profiles ===== -->
    <profiles>
        <!-- Auto-add shared-kernel where marker file exists -->
        <profile>
        <id>shared-kernel-auto</id>
        <activation>
            <file>
            <exists>.uses-shared-kernel</exists>
            </file>
        </activation>
        <dependencies>
            <dependency>
            <groupId>local.lyngberg.microservice.docker</groupId>
            <artifactId>shared-kernel</artifactId>
            <version>${revision}</version>
            </dependency>
        </dependencies>
        </profile>
    </profiles>

  </project>